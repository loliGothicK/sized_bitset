[package]
name = "tests"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dev-dependencies]
proptest = "1.1.0"
coverage-helper = "0.1.0"
sized_bitset = { path = "../sized_bitset", features = ["arbitrary"] }
